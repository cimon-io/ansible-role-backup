---
- hosts: all
  roles:
    - role: cimon-io.users
      users_accounts:
        - name: deploy
    - role: cimon-io.ruby
      ruby_global_gems: []
      ruby_versions:
        - "2.3.3"
    - role: ansible-role-backup
  vars_files:
    - ./vars.yml
