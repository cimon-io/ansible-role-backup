---
- hosts: all
  roles:
    #    - role: cimon-io.ruby
    #      ruby_user: "{{ ansible_env.USER }}"
    #      ruby_group: "{{ ansible_env.USER }}"
    #      ruby_global_gems: []
    #      ruby_versions:
    #        - "2.3.3"
    - role: ansible-role-backup
      backup_user: "{{ ansible_env.USER }}"
  vars_files:
    - ./vars.yml
