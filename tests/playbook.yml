---
- hosts: all
  pre_tasks:
    - name: install ruby
      apt:
        name: ["ruby", "ruby-dev", "build-essential"]
        update_cache: yes
  roles:
    - role: ansible-role-backup
      backup_user: root
  vars_files:
    - ./vars.yml
